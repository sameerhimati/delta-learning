# Things Sameer explicitly wants to learn — these become (:Concept {status: 'goal'})
# nodes. Segments teaching a goal concept are ALWAYS in the cut list, flagged
# "you asked to learn this". Goals the corpus does NOT cover are a feature, not a
# bug — the agent reports the gap honestly. Edit freely; re-run `make vault` after.
goals:
  - game theory
  - Bayesian statistics
  - database internals
  - context engineering for agents
  - speculative decoding
